/* global moment:false, UIkit:false */
(function() {
    'use strict';

    var dict = {
        appErrorPrefix: '<%= appName %> [ Error] ',
        appTitle: '<%= appName %>'
    };

    angular
        .module('app')

})();
